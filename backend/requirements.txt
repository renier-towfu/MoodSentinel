# MoodSentinel Backend dependencies
# Python 3.10+

# Web API
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
pydantic==2.7.1
python-dotenv==1.0.1
httpx==0.27.0

# ML / NLP runtime
torch==2.10.0
transformers==4.41.0
tokenizers==0.19.1
numpy==1.26.4
langdetect==1.0.9
regex==2024.5.15
deep-translator==1.11.4
pyabsa==2.4.1.post1

# Optional local scraper fallback:
# The public backend uses the separate browser extension for scraping.
# Install Playwright only if you restore scraper.py and set
# MOODSENTINEL_USE_EXTENSION=false.
# playwright==1.44.0
